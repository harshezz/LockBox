import { Metadata } from "next";

export const metadata: Metadata = {
  title: "DES / TDES | LockBox",
};

export default function DESLayout({ children }: { children: React.ReactNode }) {
  return children;
}
