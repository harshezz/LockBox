import { Metadata } from "next";

export const metadata: Metadata = {
  title: "MD5 | LockBox",
};

export default function MD5Layout({ children }: { children: React.ReactNode }) {
  return children;
}
