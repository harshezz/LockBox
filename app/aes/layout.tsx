import { Metadata } from "next";

export const metadata: Metadata = {
  title: "AES | LockBox",
};

export default function AESLayout({ children }: { children: React.ReactNode }) {
  return children;
}
